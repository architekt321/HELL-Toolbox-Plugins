# HyperOS Tools

HELL ROM SDK plugin `hyperos_tools` version `0.1.0`.

This initial public package validates installation, discovery, enable/disable,
loading and updates. It intentionally performs no destructive operation.
